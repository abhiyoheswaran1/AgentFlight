# v0.16.1 Release Audit

Date: 2026-06-29

Intended release version: `0.16.1`

## Scope

AgentFlight v0.16.1 is a focused packaging patch. The README references the
real finish terminal capture at `docs/assets/agentflight-finish.png`; v0.16.0
kept that asset in git but did not include it in the npm package allowlist.

## Changes

- Added `docs/assets/agentflight-finish.png` to `package.json` `files`.
- Bumped `package.json` and `package-lock.json` from `0.16.0` to `0.16.1`.
- Added a regression test that checks local README image references are covered
  by the npm package allowlist.
- Updated `CHANGELOG.md` and `AGENTFLIGHT_DEVLOG.md`.

## Verification

- `npx vitest run tests/public-positioning.test.ts`: first failed with
  missing `docs/assets/agentflight-finish.png`; passed after the package
  allowlist fix with 2 tests passing.
- `npm run typecheck`: passed.
- `npm run verify`: passed; typecheck, lint, 34 test files, 373 tests, and
  build completed successfully.
- `npm run format:check`: passed.
- `npm pack --dry-run`: passed for `agentflight@0.16.1`; dry run listed
  `docs/assets/agentflight-finish.png` and reported 214 package files.
- `npm audit --audit-level=moderate`: passed with 0 vulnerabilities.
- `npx projscan --offline doctor`: passed with health score A, 100/100.
- `npx projscan --offline preflight --mode before_commit`: caution;
  ProjScan requested careful review because the changed test file risk score was
  56.0. Health, supply-chain, and changed-file checks passed.
- `npx projscan --offline review --format json`: review verdict; no new cycles,
  risky functions, dependency changes, contract changes, taint flows, or
  dataflow risks.
- `npx agentloop verify`: passed and wrote
  `.agentloop/reports/2026-06-29-12-00-verification-report.md`.
- `npx agentloop check-gates`: warning only; task contract, verification
  report, handoff summary, harness, safety policies, git context, and git target
  passed. Remaining warning is unrelated legacy task-folder hygiene.
- `npx agentloop release-notes --write`: wrote
  `.agentloop/handoffs/2026-06-29-12-01-release-notes.md`; before commit it
  correctly noted that the release range had not been committed yet.
- `npx agentloop handoff`: wrote
  `.agentloop/handoffs/2026-06-29-12-02-pr-summary.md`.
- `npx agentloop npm-status`: before publish, local version `0.16.1`, npm
  latest `0.16.0`, registry does not yet contain local version.

## Safety

- No runtime behavior changed.
- No dependencies changed.
- No source upload, telemetry, hosted review, login, billing, GitHub App, or
  team workflow was added.
