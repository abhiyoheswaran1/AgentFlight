# AgentFlight v0.14.1 Release Audit

Date: 2026-06-27

## Scope

- Patch release for the real Baseframe readiness product capture.
- README now references `docs/assets/agentflight-baseframe-readiness.png`.
- Package allowlist now includes the new README image asset.

## Release Checks

- `npm run verify`
- `npm run format:check`
- `npm pack --dry-run`
- `npm audit`
- `npx --no-install projscan --offline doctor`
- `npx --no-install projscan --offline preflight --mode before_commit`
- `agentloop verify`
- `agentloop check-gates`
- `agentloop npm-status`

## Notes

- No CLI behavior changed in this patch release.
- The website-visible asset commit was pushed before the release bump so the
  website team could pull the image immediately.
